<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/background_primary"
    android:fitsSystemWindows="true">

    <!-- Offline Banner -->
    <com.google.android.material.card.MaterialCardView
        android:id="@+id/offlineBanner"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:visibility="gone"
        app:cardBackgroundColor="@color/status_warning"
        app:cardCornerRadius="0dp"
        app:cardElevation="4dp"
        tools:visibility="visible">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:orientation="horizontal"
            android:paddingVertical="@dimen/spacing_sm">

            <ImageView
                android:layout_width="16dp"
                android:layout_height="16dp"
                android:src="@drawable/ic_wifi_off"
                app:tint="@color/on_warning" />

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="@dimen/spacing_sm"
                android:fontFamily="@font/inter_medium"
                android:text="@string/offline_mode"
                android:textColor="@color/on_warning"
                android:textSize="@dimen/text_label_medium" />

        </LinearLayout>

    </com.google.android.material.card.MaterialCardView>

    <!-- SOS Active Indicator -->
    <com.google.android.material.card.MaterialCardView
        android:id="@+id/sosActiveIndicator"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="top"
        android:visibility="gone"
        app:cardBackgroundColor="@color/error"
        app:cardCornerRadius="0dp"
        app:cardElevation="4dp"
        app:layout_behavior="com.google.android.material.behavior.HideBottomViewOnScrollBehavior"
        tools:visibility="gone">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:orientation="horizontal"
            android:paddingVertical="@dimen/spacing_sm"
            android:paddingHorizontal="@dimen/spacing_md">

            <View
                android:id="@+id/pulseIndicator"
                android:layout_width="8dp"
                android:layout_height="8dp"
                android:background="@drawable/bg_pulse_dot" />

            <TextView
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_marginStart="@dimen/spacing_sm"
                android:layout_weight="1"
                android:fontFamily="@font/inter_semibold"
                android:text="@string/sos_active"
                android:textColor="@color/white"
                android:textSize="@dimen/text_label_medium" />

            <TextView
                android:id="@+id/tvViewSOS"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:fontFamily="@font/inter_bold"
                android:padding="@dimen/spacing_xs"
                android:text="@string/view"
                android:textColor="@color/white"
                android:textSize="@dimen/text_label_medium" />

        </LinearLayout>

    </com.google.android.material.card.MaterialCardView>

    <!-- Main Content -->
    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <!-- Navigation Host Fragment -->
        <androidx.fragment.app.FragmentContainerView
            android:id="@+id/navHostFragment"
            android:name="androidx.navigation.fragment.NavHostFragment"
            android:layout_width="0dp"
            android:layout_height="0dp"
            app:defaultNavHost="true"
            app:layout_constraintBottom_toTopOf="@id/bottomNavigation"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:navGraph="@navigation/nav_main" />

        <!-- Bottom Navigation -->
        <com.google.android.material.bottomnavigation.BottomNavigationView
            android:id="@+id/bottomNavigation"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:background="@drawable/bg_bottom_navigation"
            android:elevation="@dimen/elevation_lg"
            app:itemActiveIndicatorStyle="@style/Widget.SafeGuard.BottomNavActiveIndicator"
            app:itemIconTint="@color/nav_unselected"
            app:itemRippleColor="@color/ripple_light"
            app:itemTextAppearanceActive="@style/TextAppearance.SafeGuard.BottomNavActive"
            app:itemTextAppearanceInactive="@style/TextAppearance.SafeGuard.BottomNavInactive"
            app:itemTextColor="@color/nav_unselected"
            app:labelVisibilityMode="labeled"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:menu="@menu/bottom_nav_menu" />

    </androidx.constraintlayout.widget.ConstraintLayout>

</androidx.coordinatorlayout.widget.CoordinatorLayout>